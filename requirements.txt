networkx
rarfile  # optional: only needed for .cbr/.rar upload; .cbz/.zip/.7z work without it
torch
groq
torchvision
torch-summary
einops
scikit-image
opencv-python
pyclipper
shapely
requests
freetype-py
aiohttp
tqdm
deepl
ImageHash
kornia
backports.cached-property
huggingface_hub
transformers
py3langid==0.2.2
sentencepiece
editdistance
numpy==1.26.4
tensorboardX
protobuf<6.0.0,>=3.20.2 # `google-genai` dependancy requirement
ctranslate2
colorama
openai==1.63.0
tiktoken
httpx==0.27.2 # stop before blocking change in 0.28.0
open_clip_torch
safetensors
pandas
onnxruntime
timm
omegaconf
python-dotenv
marshmallow
cython
aioshutil
aiofiles
arabic-reshaper
pyhyphen
langcodes[data]
manga-ocr
langdetect
accelerate
bitsandbytes
pydantic==2.5.0
google-genai
rich
regex
# NOTE: rusty-manga-image-translator provides an optional faster Paddle
# detector. Its published wheels are corrupt on Windows and unreliable on
# Linux; the app falls back to the pure-Python detectors when it is absent
# (see manga_translator/detection/__init__.py). Left commented out so a
# fresh `pip install -r requirements.txt` succeeds out of the box. Enable
# only if you have a known-good wheel for your platform.
# --extra-index-url https://frederik-uni.github.io/manga-image-translator-rust/python/wheels/simple/
# rusty-manga-image-translator
python-bidi
